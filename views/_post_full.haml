- post = post_full

.post.full

  %h1= post.title

  - if post.root?
    = partial :post_right, post

  - unless post.root?
    = partial :post_right, post

  .by
    - link_to post.author.username, "/users/#{post.author.id}"
    = post.user.avatar_image
  .text
    = markup post.text

  .clear