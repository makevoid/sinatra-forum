.post
  %h2
    - link_to post.title, "/posts/#{post.id}"
  %span.by
    by
    - link_to post.author.username, "/users/#{post.author.id}"
  %br
  %span.updated_at
    %span at
    = post.updated_at.formatted