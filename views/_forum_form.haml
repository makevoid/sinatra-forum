%h1.title_form
  - if route_is_new?
    - action = "/forums"
    New Forum
  - else
    Edit: #{@forum.name}
    - action = "/forums/#{@forum.id}"

- validations_for @forum

%form.new_form{ action: action, method: "post" }
  - unless route_is_new?
    - form_method :put
  - input @forum, :name
  - input @forum, :description
  - submit "Save"