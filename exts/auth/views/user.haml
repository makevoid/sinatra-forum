%h1= @user.username

- if me?
  %nav.inpage
    - link_to "Edit profile", "/users/#{@user.id}/edit", class: "btn"

.role
  = @user.role

.avatar
  = @user.avatar

.signature
  = markdown(@user.signature)


- if File.exist? "#{Sinforum.root}/views/_ext_user.haml"
  = partial :ext_user